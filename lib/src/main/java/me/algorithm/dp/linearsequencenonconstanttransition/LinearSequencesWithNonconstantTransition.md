# Linear Sequences with non-constant Transition

Dp problem is solved on every prefix of the array just like group 2. However, transitions may not be
simple and require a linear amount of options from indices j < i.